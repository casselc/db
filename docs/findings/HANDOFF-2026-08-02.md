# Handoff — db v0.5.17 lane checkpoint 1 (2026-08-02)

Restart-safe entry point for the bounded db research/implementation lane.
Read this first, then the two findings docs.

## Refs

- Repo: `/home/chuck/ai-src/db` (`origin` = jolt-lang/db, `fork` = casselc/db).
- Branch: `agent/db-v0517-transaction-depth`, pushed to fork, HEAD `acc2b8c`.
- Worktree: `/home/chuck/ai-src/worktrees/db-v0517-transaction-depth`.
- Base: `main` @ `41da91e`. Commits on branch:
  - `224c50e` probe suite + slice-1 findings + failure artifact (run1)
  - `e7ee9e1` v0.5.17 ownership/BLOB/blocking-FFI gap refresh
  - `acc2b8c` Codex-review corrections + post-review artifact (run2)
- Environment: released jolt v0.5.17 launcher (`/home/chuck/.local/bin/jolt`),
  launcher-resolved Chez, libsqlite3 3.45.1, x86_64-linux.
- Unmerged neighbor: `agent/db-sqlite-blob-hegel` @ `53db5cf` (main +
  `clj/deps.edn` source-only dep root), deliberately not folded in.

## Commands (hermetic env `$T` = fresh mktemp dir)

```sh
# baseline (201/201 both in main checkout and worktree)
env HOME=$T/home XDG_CACHE_HOME=$T/cache XDG_CONFIG_HOME=$T/config \
    GITLIBS=$T/gitlibs JOLT_CACHE_DIR=$T/jolt-cache TMPDIR=$T/tmp \
    JOLT_PWD=$PWD jolt -M:test
# probe suite (exit 1 by design: 36 checks, 4 intended failures)
env ... jolt -A:test -m jdbc.tx-depth-probe-test
```

## Results

- Baseline: 201/201 checks, sqlite only (`JOLT_TEST_PG_URI` unset).
- Probes: 36 checks; 4 intended characterizing failures, reproduced
  identically across run1/run2 (`docs/findings/artifacts/`).
  - P1 (2): begin-boundary fail-open divergence — BEGIN reported failed
    after physically completing leaves logical depth 0 / physical inside;
    next transaction fails "cannot start a transaction within a
    transaction". Smallest failing characterization:
    `probe-p1-begin-divergence!`.
  - P5 (2): multi-statement SQL silently truncated (null `pzTail`).
- No double-completion-transition or depth-bookkeeping defect reproduces
  (P3/S1–S6 exact traces, P4 reentrant depth samples).
- Codex review (`--base main`, checkpoint): three P2 findings, all verified
  against source/runtime probe, all corrected in `acc2b8c`.

## Decisions

- Probe file intentionally fails and stays out of the `:test` alias; wire it
  in only after the begin-boundary fix contract is decided.
- Slice 1/2 changed no library code.
- Post-release fork FFI features (loans, ranged transfers, native-error
  capture, exact widths) are not in released v0.5.17; do not adopt.
- `read-blob`'s `sqlite3_errcode` pattern must stay (errno-based capture
  would not replace it).

## Open items for the integration session (priority order)

1. Decide the begin-boundary contract: on begin-command failure, either
   best-effort counter-command (ROLLBACK / RELEASE-less savepoint path) or
   fail-closed marker; `sqlite3_get_autocommit` binding enables verified
   physical-state recovery. Then convert P1's two checks to the fixed
   contract and add them to `:test`.
2. Decide multi-statement policy (document single-statement contract vs
   reject non-empty tail vs prepare tail chain). P5 checks are tripwires.
3. Optional additions: `sqlite3_busy_timeout` (opt-in bounded wait),
   `sqlite3_extended_errcode`.
4. Fold `53db5cf` if/when the hegel gate runs; run `jolt hegel-test`.
5. PostgreSQL lane never executed; `db.pg` begin path unprobed.

## Nonclaims

- No PostgreSQL execution. No hegel run. No SMT re-run (executable matrix
  supersedes the v0.5.11-era model claims). Probes ran on Linux x86_64 only;
  macOS/Windows lanes unexecuted. The jolt reader silent-truncation
  observation (unmatched top-level `)` drops later forms without error) is
  noted for a jolt-upstream ticket, not fixed here.
